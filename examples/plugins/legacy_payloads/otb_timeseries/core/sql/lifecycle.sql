-- =================================================================
-- 生命周期管理 (Lifecycle Management)
-- =================================================================
-- 
-- 注意：以下功能已独立为单独的插件：
-- 
-- 1. 异常检测功能 → 请安装 otb_analytics 插件
--    - detect_anomalies()
--    - detect_anomalies_zscore()
--    - detect_anomalies_iqr()
--
-- 2. 数据快照与回滚 → 请安装 otb_snapshot 插件
--    - create_snapshot()
--    - list_snapshots()
--    - rollback_to_snapshot()
--    - drop_snapshot()
--
-- 此文件保留用于未来的生命周期管理功能扩展。
-- =================================================================

-- 预留：未来可在此添加其他生命周期管理功能
